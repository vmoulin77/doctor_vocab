package vincent.moulin.vocab.entities;

public class WordFrench extends Word {

    public WordFrench(String content, int mode, boolean isAccelerated,
            int indicePrimary, int indiceSecondary,
            int timestampLastAnswer) {
        super(content, mode, isAccelerated, indicePrimary, indiceSecondary,
                timestampLastAnswer);
    }
    
}
